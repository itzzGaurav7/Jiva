import 'package:flutter/material.dart';

class AnalyBody extends StatelessWidget {
  const AnalyBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Container(
          color: Colors.orange,
        ),
      ),
    );
  }
}
