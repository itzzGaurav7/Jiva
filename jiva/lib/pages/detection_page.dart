import 'package:flutter/material.dart';

class DetectionBody extends StatelessWidget {
  const DetectionBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Container(
          color: Colors.purple,
        ),
      ),
    );
  }
}
